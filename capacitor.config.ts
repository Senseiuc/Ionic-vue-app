import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.atticgolden.vueIonicMemories',
  appName: 'VueIonic Memories',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
